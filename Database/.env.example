# Async DSN used by SQLAlchemy + tests
DATABASE_URL=postgresql+asyncpg://auto_workflow:auto_workflow@localhost:5435/auto_workflow

# Sync DSN used by scripts/migrate.py (psycopg)
DATABASE_URL_SYNC=postgresql://auto_workflow:auto_workflow@localhost:5435/auto_workflow
