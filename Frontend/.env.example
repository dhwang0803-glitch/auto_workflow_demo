# Dev token used by lib/api.ts as Bearer for backend calls.
# Generate via API_Server: POST /api/v1/auth/login → access_token.
NEXT_PUBLIC_DEV_TOKEN=

# Optional: where the dev rewrite proxies /api/* (defaults to 127.0.0.1:8000).
# API_PROXY_TARGET=http://127.0.0.1:8000
